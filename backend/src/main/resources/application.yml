#Los perfiles DEV Y QA funcionan con h2.
#Perfiles DEV, QA, PROD
image:
  directory:
    event: images/events/
    profile: images/profiles/

spring:
  profiles:
    active: prod

  datasource:
    url: jdbc:mysql://10.0.0.15:3306/db_polo_it
    username: admin
    password: noCountry_2024
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop


  mail:
    host: smtp.gmail.com
    port: 587
    username: ${email}
    password: ${email_password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

#email de ejemplo para probar /resetPassword
mail: ${email_user_ejemplo}